<div class="container">
    <div class="product-category">
        <h2>
            {{message}}
        </h2>
    </div>
    <div class="product-container mb-3">
        {{# each products}}
            {{> productrow products=this }}
            {{!-- productrow in partials --}}
        {{/each}}
    </div>
</div>